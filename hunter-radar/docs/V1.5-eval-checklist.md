># V1.5 评估清单 (M7-t9 落地)

> **状态**:🟡 V1.5 准备(M7-t9 落地,与 v1.5 freeze 并行)
> **关联**:v1.5 OpenAPI freeze (m7t7) + V1.5.1 freeze 待定
> **作者**:M7 接力 session

---

## 一、背景

V1.5 准备阶段(M7)积累的多项增量,需在 V1.5 真实落地前评估:
- 是否纳入 V1.5.1 freeze?
- 是否独立 V1.6?
- 是否后置到 V2.0?

---

## 二、候选 A 权重(M7-t4 落地)

| 项 | 详情 | 评估建议 |
|---|---|---|
| 候选 A 权重(stock `{options:25, short:40, divergence:20, insider:15}`) | m7t4 真实数据集评估 | 🟡 **V1.6 重新评估**(100+ 事件 + 真实 ETL) |
| 当前决策 | 保持 v1.0 默认权重(Mann-Whitney p=0.3827 不显著) | ✅ 沿用至 V1.5 |
| 触发重测条件 | 新数据集 p<0.05 + delta_f1>+0.05 | V1.5.1 触发评估 |

**结论**:V1.5 沿用 v1.0 权重。候选 A 权重定义保留在 `m7t4_run_backtest_v30_final.py`,V1.6+ 重启评估。

---

## 三、BD-086 双签(M7-t2 落地)

| 项 | 详情 | 评估建议 |
|---|---|---|
| 31 事件 reviewer_signoff 双签 | m7t2 sandbox_stub 占位 | 🟢 **生产前替换** review_mode=sandbox_stub → prod_signoff_v1 |
| CI 校验 prod ≠ sandbox_stub | m7t8 CI docs job 校验 | ✅ 沿用 |
| Audit log 落 JSONL | `data/backtest_event_goldset.signoff_audit.jsonl` 31 行 | ✅ 沿用 |

**结论**:V1.4 上线前需逐事件替换双签 stub 为真实签名。

---

## 四、BD-085 真实数据集(M7-t3 落地)

| 项 | 详情 | 评估建议 |
|---|---|---|
| `etl/backtest_dataset_real.py` 沙箱 stub | 4220 行 JSONL | 🟡 **V1.5 切 PG** + asyncpg + 真实 FINRA RegSHO |
| `data/backtest_dataset_real.sandbox.jsonl` | 31 ticker × 90 天 × OHLCV + short_volume + form4 | ✅ 沙箱保留 |
| 真实 ETL 切换步骤 | `backtest_dataset_real.py` → `backtest_dataset_pg.py` | V1.5.1 |

**结论**:V1.5 切真实 PG 落地,V1.5.1 freeze 前完成。

---

## 五、BD-087 v3.0-final 校准(M7-t4 落地)

| 项 | 详情 | 评估建议 |
|---|---|---|
| 决策:保持 v1.0 默认权重 | p=0.3827 不显著 | ✅ V1.5 沿用 |
| `docs/BD-087-calibration-report-v3.0-final.md` | 9 章节,254 行 | ✅ 归档 |
| `docs/BD-087-calibration-run-m7t4.json` | Mann-Whitney U 输出 | ✅ 归档 |
| 候选 A 留存至 V1.6 评估 | 100+ 事件重测 | V1.6 |

**结论**:R-27 风险已解除。候选 A 重测待 V1.6。

---

## 六、EDGAR fulltext search(M7-t5 落地)

| 项 | 详情 | 评估建议 |
|---|---|---|
| `etl/edgar_fulltext.py` 沙箱 stub | 86 filings × 27 ticker | 🟡 **V1.5.1 真实 httpx** + EDGAR full-text search API |
| 不暴露为 API | m7t7 v1.5 freeze 不含 EDGAR 端点 | 🟡 V1.5.1 评估 `/api/v1/edgar/search` |
| 与 m6t8 8-K 集成 | 8-K Item 8.01 fixture 与 EDGAR stub 复用 CATEGORY_KEYWORDS | ✅ |

**结论**:V1.5.1 评估 EDGAR 真实接入 + 是否新增公开 API。

---

## 七、Stripe webhook 签名校验(M7-t6 落地)

| 项 | 详情 | 评估建议 |
|---|---|---|
| 沙箱显式 `signature_skipped=true` | 200 + sandbox_skip + warning | ✅ |
| 真实模式 `stripe.Webhook.construct_event` | secret + SDK 校验 | ✅ |
| SDK 不可用 503 | `signature_check_unavailable` | ✅ |
| R-31 解除 | 不 mock 200 伪装 | ✅ |

**结论**:V1.4 上线前生产环境必须设 `STRIPE_WEBHOOK_SECRET`。

---

## 八、OpenAPI v1.5 freeze(M7-t7 落地)

| 项 | 详情 | 评估建议 |
|---|---|---|
| 48 endpoints / 40 paths / 13 tags | 4 admin 新增 + 1 webhook 行为变更 | ✅ v1.5 freeze 落地 |
| Admin 端点暂免鉴权 | R-34 风险 | 🟡 V1.5.1 加 admin role + JWT |
| Admin 暴露 ETL trigger | R-35 风险 | 🟡 V1.5.1 IP 白名单 + rate limit |
| FE-010 同步 | `docs/FE-010-changelog-v1.5.md` | ✅ |

**结论**:V1.5 freeze 落地。R-34/35 待 V1.5.1 缓解。

---

## 九、PWA + CI(M7-t8 落地)

| 项 | 详情 | 评估建议 |
|---|---|---|
| `.github/workflows/ci.yml` 6 jobs | backend / openapi-drift / frontend / secrets-check / webhook / docs | ✅ |
| vite.config.ts VitePWA + Workbox 5 类 | 已有 | ✅ |
| lighthouserc.cjs 性能 / a11y / SEO 阈值 | 已有 | ✅ |
| VAPID 私钥/公钥 + claims_email | config.py 已有 | 🟡 V1.4 上线前设真实密钥 |
| Sentry DSN | config.py 已有 | 🟡 V1.4 上线前设真实 DSN |

**结论**:V1.4 上线前需设真实 VAPID + Sentry 环境变量。

---

## 十、BD-088 ETF 申赎代理(M7-t9 落地 stub)

| 项 | 详情 | 评估建议 |
|---|---|---|
| `app/services/etf_proxy.py` stub | EtfBasket / EtfOrder + 套利检测 | 🟡 V1.5.1 接 Bloomberg + AP |
| 不暴露 API | R-37 风险 | 🟡 V1.5.1 新增 3 端点 |
| 套利检测逻辑简化 | R-38 风险 | V1.5+ |
| 现金流风控 | R-39 风险 | V1.5+ |

**结论**:V1.5.1 freeze 待 AP 集成 + 新增 3 端点。

---

## 十一、用户增长埋点(M7-t9 落地 stub)

| 项 | 详情 | 评估建议 |
|---|---|---|
| `app/services/analytics.py` stub | 10 事件 + ring buffer + funnel | 🟡 V1.5.1 接 postHog + ClickHouse |
| 不暴露 API | R-40~43 风险 | 🟡 V1.5.1 新增 `POST /api/v1/analytics/events` |
| 现有 6 trigger 集成 | 待 V1.5+ 接入 | V1.5+ |
| 隐私合规 | GDPR / CCPA | V1.5+ opt-in 弹窗 |

**结论**:V1.5.1 freeze 待接 postHog + 新增 events 端点。

---

## 十二、V1.5.1 freeze 候选清单

汇总需 V1.5.1 freeze 的端点 + 行为变更:

| 端点 / 变更 | 触发 | 优先级 |
|---|---|---|
| `/api/v1/admin/*` 鉴权 | R-34 缓解 | 🔴 高 |
| `/api/v1/admin/*` IP 白名单 | R-35 缓解 | 🔴 高 |
| `/api/v1/edgar/search` (GET) | m7t5 + V1.5.1 | 🟡 中 |
| `/api/v1/etf/{ticker}/basket` (GET) | m7t9 + BD-088 | 🟡 中 |
| `/api/v1/etf/orders` (POST / GET) | m7t9 + BD-088 | 🟡 中 |
| `/api/v1/etf/orders/{order_id}` (GET) | m7t9 + BD-088 | 🟡 中 |
| `/api/v1/analytics/events` (POST) | m7t9 + 埋点 | 🟢 低 |
| 候选 A 权重切换(条件触发) | 100+ 事件 p<0.05 + delta_f1>+0.05 | 🟢 低 |

**预计 V1.5.1 freeze 后**:48 → 56+ endpoints。

---

## 十三、本日记忆(M7-t9)

1. **V1.5 评估清单落地**:13 个 V1.5 增量项逐项评估
2. **V1.5 沿用 v1.0 权重**:候选 A 待 V1.6
3. **V1.5.1 freeze 待定**:8 项端点 + 行为变更
4. **R-37~43 风险新增**:BD-088 / 埋点 / 隐私
5. **服务 stub 落地**:`etf_proxy.py` + `analytics.py` 不破坏 v1.5 freeze

---

> **下一步**:M7-t10(M7-handoff + V1.4 final 收尾报告)